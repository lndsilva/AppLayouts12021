<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.local.applayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppLayouts">
        <activity android:name=".Varios_Layouts_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LayoutScrollView_Activity"></activity>
        <activity android:name=".LayoutFrame_Activity">

        </activity>
        <activity android:name=".LayoutTable_Activity" />
        <activity android:name=".LayoutRelativo_Activity" />
        <activity android:name=".LayoutHorizontal_Activity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>